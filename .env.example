# travelNepal Environment Configuration for XAMPP
# Copy this file to .env and update the values for your local setup

# Admin Credentials (Required)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password_here

# Database Configuration for MySQL/XAMPP
# Option 1: Use DATABASE_URL (if you prefer URL format)
# DATABASE_URL=mysql://username:password@localhost:3306/travel_nepal

# Option 2: Use individual database parameters (recommended for XAMPP)
DB_HOST=localhost
DB_NAME=travel_nepal
DB_USER=root
DB_PASS=
DB_PORT=3306

# Environment Settings
ENVIRONMENT=development

# Optional: Set to true if using HTTPS in production
# PRODUCTION_MODE=false